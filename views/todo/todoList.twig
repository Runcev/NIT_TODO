{% extends 'layout.twig' %}

{% block body %}

    <h3>{{ title }}</h3>

    <table class="table table-striped">
        <tr>
            <th>
            </th>
            <th>
                Name
            </th>
            <th>
                Date (yyyy.mm.dd)
            </th>
        </tr>
        {% for todo in todos %}
            <tr>
                <td width="20px">
                    <a href="{{ todo.url }}/edit" data-toggle="tooltip" title="Edit"><i class="far fa-edit"></i></a>
                </td>
                <td>
                    {{ todo.name }}
                </td>
                <td>
                    {{ todo.date|date('Y.m.d') }}
                </td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}
