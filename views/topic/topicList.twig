{% extends 'layout.twig' %}

{% block body %}

    <h3>{{ title }}</h3>

    <table class="table table-striped">
        {% for entitytype in entitytypes %}
            <tr>
                <td width="20px">
                    <a href="{{ entitytype.url }}/edit" data-toggle="tooltip" title="Edit"><i class="far fa-edit"></i></a>
                </td>
                <td>
                    {{ entitytype.name }}
                </td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}
