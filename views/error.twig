{% extends 'layout.twig' %}

{% block body %}


    <div style="text-align: center; margin-top: 50px">{{ message }}</div>
    {#
    <h2>{{ error.status }}</h2>
    <pre>{{ error.stack }}</pre>
    #}
{% endblock %}
